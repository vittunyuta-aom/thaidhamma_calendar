Start a local HTTP server to preview the calendar app, then open it in the browser.

Use Python's built-in HTTP server (python -m http.server 8080) from the project directory: C:\Users\Saphita\Documents\Vittunyuta\repos\thaidhamma_calendar

Run the server in the background, then open http://localhost:8080 in the default browser using `start http://localhost:8080` (Windows).

Tell the user the server is running and how to stop it (Ctrl+C or kill the process).
